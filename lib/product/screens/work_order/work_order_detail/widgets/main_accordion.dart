import 'package:flutter/material.dart';

class MainAccordion extends StatelessWidget {
  const MainAccordion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
